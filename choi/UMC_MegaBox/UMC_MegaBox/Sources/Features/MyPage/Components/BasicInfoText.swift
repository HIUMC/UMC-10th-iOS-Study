import SwiftUI

struct BasicInfoText: View {
    var body: some View {
        HStack {
            Text("기본정보")
                .font(.pretendardBold18)
                .foregroundColor(Color(.black))
            Spacer()
        }
    }
}
